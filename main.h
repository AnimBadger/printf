#ifndef __MAIN__
#define __MAIN__

#include <stdarg.h>
#include <unistd.h>
#include <stdlib.h>

int _puts(char *str);
int _putchar(int c);
int _strlen(char *str);

int _printf(const char *format, ...);

#endif
